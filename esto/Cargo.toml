[package]
name = "esto"
version = "0.1.0"
authors = ["Ross Jones"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
esto-core = { path = "../esto-core" }