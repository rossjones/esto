syntax = "proto3";
package store;

service Storer {
    rpc StoreRecord (StoreRequest) returns (StoreReply);
}

message StoreRequest {
   string name = 1;
}

message StoreReply {
    string message = 1;
}